import { RemoveActionColPipe } from './remove-action-col.pipe';

describe('RemoveActionColPipe', () => {
  it('create an instance', () => {
    const pipe = new RemoveActionColPipe();
    expect(pipe).toBeTruthy();
  });
});
